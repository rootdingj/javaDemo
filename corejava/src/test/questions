1、
	int a2 = -12 >>> 2;                         //a2变量的取值为1073741821;  
		两点：int类型（***）  
			     无符号右移
			     
------------------------------------------------------ 
2、
		char c = '\u0012';
       //	char c1 = '\u12';  //就算是注释了也通不过编译，为什么？
		/*\u....可能表示一个有意义的字符，如表示注释*/....可能会改变你本意的编码，使得通不过编译
		 *
		 * Unicode可同时包含65536个字符，ASCII/ANSI只包含255个字符，实际上是Unicode的一个子集。
		 * Unicode字符通常用十六进制编码方案表示，范围在'\u0000'到'\uFFFF'之间(4个16进制)。
		 * \u0000到\u00FF表示ASCII/ANSI字符。\u表示这是一个Unicode值.
		 * */
		 
		 
	----------------------------------------------------	 

		 
		 
		 
		 
		 