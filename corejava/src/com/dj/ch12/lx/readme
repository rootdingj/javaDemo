1.实现 龟兔赛跑
提示：可以采用Math.random()取得0~1之间的随机数模拟比赛进程，
如总距离为100米，在随机数0~0.3之间代表兔子跑，
每次跑2米，在0.3~1之间代表乌龟跑，每次跑1米，
先跑完100米者为胜利者

Race类：产生RabbitAndTurtle的两个实例，分别代表兔子和乌龟
RabbitAndTurtle类：继承Thread类，实现赛跑的逻辑

扩展：

--------------------------------------
2、写Storage类
要求：可以保存1个int类型的数据

写Counter类
要求：继承Thread类，作用为往Storage对象中依次保存0~99的数据(每次覆盖)

写Printer类
要求：继承Thread类，作用为取出Storage对象中保存的数据并输出

写Test类
要求：分别创建Counter类的对象和Printer类的对象并启动线程类




